#!/system/bin/sh
# Thin wrapper so `su -c quietctl ...` works from anywhere. A real file rather
# than a symlink, because zip-stored symlinks do not survive every installer.
export LUNE_MODDIR=/data/adb/modules/lune_bridge
exec "$LUNE_MODDIR/bin/quietctl" "$@"
